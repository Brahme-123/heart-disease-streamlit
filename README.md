# heart-disease-streamlit
The Heart Disease Prediction Web Application is a machine learning–based system designed to predict whether a person is likely to have heart disease based on medical parameters. The application uses a trained Support Vector Machine (SVM) model and provides predictions through an interactive Streamlit web interface.  
